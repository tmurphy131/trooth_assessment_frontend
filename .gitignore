# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
android/key.properties

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ─────────────────────────────────────────────
# Additional Flutter / Tooling
# ─────────────────────────────────────────────
.packages
coverage/
*.lcov
flutter_*.png
fvm_config.json
melos_workspace.yaml
.fvm/

# Optional: ignore desktop platforms if not in active use (uncomment as needed)
# linux/
# macos/
# windows/

# Web build output (keep web/ source)
web/build/
web/.dart_tool/

# VSCode settings (uncomment to ignore) and common editor caches
.vscode/
.idea/
.fleet/

# Firebase / secrets (uncomment if you do NOT want to commit these)
# **/google-services.json
# **/GoogleService-Info.plist

# Fastlane / CI artifacts
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/

# Crash / symbol outputs
*.symbols
*.map

# Generated plugin registrant (already caught but explicit for clarity)
**/GeneratedPluginRegistrant.*
# Build logs and temp files
Build Runner*.txt
*.docx
~$*

# Numbered duplicate files (created by macOS conflicts)
*\ [0-9]*
*\ 2.*
*\ 3.*
*\ 4.*
*\ 5.*
*\ 6.*
*\ 7.*
*\ 8.*
*\ 9.*
*\ 10.*